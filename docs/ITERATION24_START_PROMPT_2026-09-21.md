@GitHub Proceed with Iteration 24 of the New Daily AI Brief greenfield implementation in `gttome/New-Daily-AI-Brief`.

Use current `main` as the source of truth. Before changing anything, independently verify its SHA and CI, verify Iteration 23 is completely closed and passing, and read:

- `docs/ITERATION24_HANDOFF_2026-09-21.md`
- `docs/ITERATION23_AFTER_ACTION_2026-09-21.md`
- `evidence/iteration23/synthetic-shadow-production-integration-execution-executor-binding-authorization-review-evidence.json`
- `docs/SCHEMA_VERSION_POLICY.md`
- `docs/ITERATION_START_PACKAGE_STANDARD.md`

The Iteration 24 handoff is the controlling specification. Do not begin if Iteration 23 closure is pending or `iteration24_ready=false`. Establish the baseline solely from reconciled current repository records, never a remembered SHA or chat history.

Implement Iteration 24 only: a deterministic synthetic-only executor-binding authorization decision gate consuming the exact locked Iteration 23 authorization-review artifact. The current repository must remain blocked because that source artifact is blocked. A completed synthetic review is insufficient without a separate explicit synthetic decision record bound to its complete ordered evidence inventory and all required upstream identities. The decision grants no real production authority and must never bind or invoke a real executor.

Preserve merged Iterations 1–23, all locked artifacts and identities, the lifecycle/state machine, canonical `start_daily_brief(date, mode)`, single RunEngine owner, leases/idempotency, content-addressed locks, dependency invalidation, completion primitives, and incident/recovery receipts. Add exactly one bounded decision-only path. Preserve exact Iteration 23 review/policy/manifest/separate-review/ordered-evidence identities, exact Iteration 22 rehearsal/policy/manifest/separate-rehearsal/ordered-receipt identities, Iterations 21/20 executor-binding provenance, and every required transitive Iterations 19–9 identity. Do not reexecute or rebuild prior completed work.

Prove deterministic replay, exact upstream binding, stable classifications/reasons, fail-closed stale/corrupted inputs, reordered/duplicated/missing/substituted/corrupted evidence inventories, unsupported versions, changed identities, and separate decision-record necessity. Reject every real endpoint, binding/invocation capability, executable command/step, credential, target, authority, mutation flag, or unapproved paid dependency. Prove targeted evaluation, decision-evidence, and final-artifact failure recovery; fresh-engine/no-chat resume; zero locked Iterations 1–23 reexecution; zero Iteration 23 artifact rebuild; zero unrelated record rewrites; and zero full-pipeline restart.

Do not infer any authorization, executor, credential, target/environment, rollback authority, cost approval, cutover/decommission/publication approval, or missing identity from chat. Do not implement, bind, or invoke a real executor; use/store production credentials; contact a real target; grant actual production authority; execute rollback; mutate the private Command Center; implement final UI; deploy GitHub Pages or a public ChatGPT Site; change live/public URLs; perform real public-route verification; create/modify/enable/disable/run production schedules; change subscriber delivery; migrate legacy content; cut over; route readers to greenfield; decommission legacy; publish to production; or modify/interfere with `gttome/Daily-AI-Brief`.

Do not add a separately billed completion/storage/OpenAI API, paid deployment/hosting API, or other incremental paid production dependency. Any such requirement lacking explicit repository-authoritative zero-cost approval must fail closed.

Continue through implementation, deterministic replay, injected-failure recovery, complete regression testing, PR/CI, exact-candidate merge, and independent post-merge main verification without stopping for routine decisions. Completion requires three consecutive independent synthetic/shadow decision-only runs satisfying the handoff exit gate.

Create and merge all four mandatory Iteration 24 closure artifacts, including the Iteration 25 handoff and separate standalone Iteration 25 start prompt. Reconcile closure and main CI before reporting Iteration 25 ready. Report final main SHA, PRs, CI, test counts, three-run results, recovery/anti-rework evidence, and links to every closure/startup document.
