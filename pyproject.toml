[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "new-daily-ai-brief"
version = "0.5.0"
description = "Greenfield Daily AI Brief control plane through deterministic shadow reader rendering"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
